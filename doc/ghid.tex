\section{Ghid utilizare}
\subsection{Utilizarea simulatorului}

Dat fiind faptul ca OpenOGEHL a pornit ca a aplicatie de sine statatoare, fiind numai apoi integrate in cadrul suitei SimpleSim. acesta prezinta doua modalitati de functionare. Pe de o parte utilizatorul va avea la dispozitia acestuia o aplicatie grafica ce va permite procesarea unor fisiere de tip trace obtinute  urma rularii suitei de teste standard Stanford \cite{soac}, iar pe de alta parte va mai avea posibilitatea de a-l ca modul pentru dezvoltarea de aplicatii in framework-ul SimpleSim.

\subsubsection{Folosind OpenOGEHL clasic}
Asa cum am mentionat anterior OpenOGEHL a fost conceput ca o aplicatie separata. In versiunea originala aceasta are drept date intrare asa numitele fisiere \textit{trace} care are redau secvența de salturi facute (sau nu) sau, mai bine zis traseul parcurs de program. Aceste trasee au fost obținute în urma rularii suitei de teste standard Stanford ce contin diverse aplicatii precum fbubble.ins (care reda implementarea algoritmului de sortare Bubble Sort). Forma generala a unui salt este de forma <TipSalt> <AdresaCurenta> <AdresaDestinatie>\cite{soac} (ex: BS
2 110, unde adresa curenta denota de fapt valoarea registrului PC).
Mai jos se pot observa mnemonicele folosite pentru salturi:

\begin{table}[H]
	\centering
	\renewcommand{\arraystretch}{1.3}

	\begin{tabular}{|>{\centering\arraybackslash}m{3cm}|m{9cm}|}
		\hline
		\textbf{Mnemonica} & \textbf{Semnificația} \\
		\hline

		BT & Branch Taken (saltul se face) \\
		\hline

		BN & Branch Not Taken (saltul nu se face) \\
		\hline

		BS & Branch to Subroutine (salt de tip call) \\
		\hline

		BM & Return \\
		\hline

		BR & Branch (salt necondiționat) \\
		\hline
	\end{tabular}

	\caption{Mnemonicele folosite}
	\label{tab:branch_types}
\end{table}

Astfel, secventa data ca exemplu poate fi citita drept ``Salt la subrutina incepand de la adresa 110''.

Cand utilizatorul va lansa programul in executie va avea la dispoziția acestuia o fereastra principala de control si informatii ale predictorului. Astfel, ea este divizata in urmatoarele regiuni si functionalitati de la stanga la dreapta: trei
campuri de text folosite pentru configurarea parametrilor predictorului de salturi (numarul de tabele folosite, dimensiunea unei tabele si numarul de biti per contor), informații privind fișierul de trace folosit, rezultatul predictiei si valorile curente ale parametrilor predictorului; trei butoane de control pentru a determina paleta de culori ai interfeței grafice (“Dark” si “Light”),
a incarca fisierul de trace si a începe simularea.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/dark_ui.png}
	\caption{Interfata grafica setata pe modul Dark}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/light_ui.png}
	\caption{Interfata grafica setata pe modul Light}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/load_trace.png}
	\caption{Incarcand un fisier de tip trace}
\end{figure}

\subsubsection{Folosind OpenOGEHL ca parte a SimpleSim}
Ca parte a efortului de cercetare si dezvoltare a arhitecturilor de calcul s-a propus integrarea aplicatiei OpenOGEHL deja existente ca parte a suitei de simulatoare SimpleSim. Scopul acestei sectiuni va fi de a ilustra succint integrarea amintita in timp ce structura SimpleSim va fi explicata in capitolul urmator (Dezvoltarea simulatorului).

Structural vorbind, codul sursa pentru SimpleSim si OpenOGEHL este impartit in directoare diferite (a se consulta imaginea atasata) legatura dintre ele fiind realizata de catre fisierul \textbf{CMakeLists} ce serveste drept \textit{build system} al framework-ului SimpleSim (continut in directorul simplesim). Pe scurt, acel fisier face parte dintr-un sistem de procesare a fisierelor sursa C aferente proiectului cu scopul de a obtine intr-un final fisierul executabil, mai multe detalii fiind regasite in capitolul urmator.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/proj_layout.png}
	\caption{Structura ierarhica de directoare a OpenOGEHL}
\end{figure}

Din perspectiva utilizatorului este de ajuns pentru folosi fisierul antet \textit{bp.h} ce defineste interfata oferita de predictorul de salturi implementat de tip O-GEHL. Mai jos se vor putea observa fisierele incluse pentru realizarea simulatorului sim-bpred:

\begin{verbatim}
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include "host.h"
#include "misc.h"
#include "machine.h"
#include "regs.h"
#include "memory.h"
#include "loader.h"
#include "syscall.h"
#include "dlite.h"
#include "options.h"
#include "stats.h"
#include "bpred.h"
#include "sim.h"
/* OGHEL predictor: gNumOfTables, gTableSize, gCounterLen */
#include "bp.h"
\end{verbatim}

Stiind acum acestea se pune problema de a rula simulator ce incorporeaza predictorul de salturi dezvoltat de noi. Spre norocul utilizatorului, acesta are de asemenea la dispozitia sa un mic script in BASH ce compileaza si ruleaza cu o configuratie implicit selectata.

\begin{verbatim}
	#!/bin/bash

	# Usage:
	#   ./gen                        - configure (cmake)
	#   ./gen b                      - build
	#   ./gen <benchmark> r          - run default config for benchmark
	#   ./gen <benchmark> c1..c9     - run named config for benchmark
	#   ./gen <benchmark> all        - run all configs for benchmark
	#
	# Benchmarks: applu | cc1 | mgrid | compress | ijpeg
	#
	# OGHEL predictor params: -bpred:oghel <num_tables> <table_size> <counter_len>
	# Defaults:               num_tables=7  table_size=2048  counter_len=4

	BASE_CMD="./build/sim-bpred -max:inst 500000000 -nice 1 -bpred oghel"

	# ---- benchmark selection ----
	BENCHMARK="${1}"
	case "$BENCHMARK" in
	applu)    SS_FILE="applu.ss";       IN_FILE="applu.in"   ;;
	cc1)      SS_FILE="cc1.ss";         IN_FILE="1stm.i"   ;;
	mgrid)    SS_FILE="mgrid.ss";       IN_FILE="mgrid.in"   ;;
	compress) SS_FILE="compress95.ss";  IN_FILE="bigtest.in" ;;
	ijpeg)    SS_FILE="ijpeg.ss";       IN_FILE="vgio.ppm"     ;;
	b)
	cmake --build build -j 12
	exit 0
	;;
	"")
	rm -rf build
	cmake -S . -B build -G "Unix Makefiles"
	exit 0
	;;
	*)
	echo "Unknown benchmark: $1"
	echo "Usage: $0 [applu|cc1|mgrid|compress|ijpeg] [r|c1..c9|all]"
	exit 1
	;;
	esac

	INPUT="./inputs/ss_files/${SS_FILE}"
	STDIN="./inputs/in_files/${IN_FILE}"
	mkdir -p "./results/${BENCHMARK}"

	run_config() {
		local tag="$1"
		local num_tables="$2"
		local table_size="$3"
		local counter_len="$4"
		echo "=== ${BENCHMARK}/${tag}: num_tables=$num_tables  table_size=$table_size  counter_len=$counter_len ==="
		$BASE_CMD \
		-bpred:oghel $num_tables $table_size $counter_len \
		-redir:sim  "./results/${BENCHMARK}/${tag}_simout.res" \
		-redir:prog "./results/${BENCHMARK}/${tag}_progout.res" \
		"$INPUT" < "$STDIN"
	}

	CMD="${2:-r}"

	case "$CMD" in
	r)   run_config default 7 2048 4 ;;
	# ---------- named configurations ----------
	#                       num_tables  table_size  counter_len
	c1)  run_config c1   4   512   3 ;;  # min tables, small table, short counters
	c2)  run_config c2   4  2048   4 ;;  # min tables, default size
	c3)  run_config c3   7   512   3 ;;  # default tables, small table, short counters
	c4)  run_config c4   7  2048   4 ;;  # default (baseline)
	c5)  run_config c5   7  2048   5 ;;  # default tables/size, long counters
	c6)  run_config c6   7  8192   4 ;;  # default tables, larger table
	c7)  run_config c7  12  2048   4 ;;  # max tables, default size
	c8)  run_config c8  12  8192   4 ;;  # max tables, larger table
	c9)  run_config c9  12  8192   5 ;;  # max tables, larger table, long counters
	all)
	for cfg in c1 c2 c3 c4 c5 c6 c7 c8 c9; do
	"$0" "$BENCHMARK" "$cfg"
	done
	;;
	*)
	echo "Unknown command: $CMD"
	echo "Usage: $0 [applu|cc1|mgrid|compress|ijpeg] [r|c1..c9|all]"
	exit 1
	;;
	esac
	elif [[ "$1" == "all" ]]; then
	for cfg in c1 c2 c3 c4 c5 c6 c7 c8 c9; do
	"$0" "$cfg"
	done

	else
	echo "Unknown option: $1"
	echo "Usage: $0 [b|r|c1..c9|all]"
	exit 1
	fi

\end{verbatim}

Daca ar fi sa analizam un pic acest script vom putea observa ca acesta asteapta argumente pozitionale la intrare (adica argumente care sunt separate logic intre ele prin pozitionarea acestuia in cadrul comanzii intreagi de excutat; a se face contrast cu argumentele bazate pe flag.).Daca niciunul nu este pasat scriptului, atunci acesta implicit foloseste CMake pentru a general fisierele necesare compilarii, compilare ce va avea loc daca utilizatorul introduce optiunea "b" (de la \textbf{build}). Pentru a rula simularea in sine se poate alege una dintre cele noua configuratii predefinite ce tin de numarul tabelelor, marimea unei tabele si dimensiunea contorului aferent unei tabele. Mai multe detalii despre acestea vor fi oferita in capitolul urmator.

Lansand din linia de comanda programul \textit{sim-bpred} fara niciun argument vom fi intampinati de urmatorul mesaj ajutator care evidentiaza printre altele si optiunile valabile pentru predictorul OGEHL:

{\small
\begin{verbatim}
	sim-bpred: SimpleScalar/PISA Tool Set version 3.0 of November, 2000.
	Copyright (c) 1994-2000 by Todd M. Austin.  All Rights Reserved.
	This version of SimpleScalar is licensed for academic non-commercial use only.

	Usage: ./build/sim-bpred {-options} executable {arguments}

	sim-bpred: This simulator implements a branch predictor analyzer.

	#
	# -option        <args>           #    <default> # description
	#
	-config          <string>         #       <null> # load configuration from a file
	-dumpconfig      <string>         #       <null> # dump configuration to a file
	-h               <true|false>     #        false # print help message
	-v               <true|false>     #        false # verbose operation
	-d               <true|false>     #        false # enable debug message
	-i               <true|false>     #        false # start in Dlite debugger
	-seed            <int>            #            1 # random number generator seed (0 for timer seed)
	-q               <true|false>     #        false # initialize and terminate immediately
	-chkpt           <string>         #       <null> # restore EIO trace execution from <fname>
	-redir:sim       <string>         #       <null> # redirect simulator output to file (non-interactive only)
	-redir:prog      <string>         #       <null> # redirect simulated program output to file
	-nice            <int>            #            0 # simulator scheduling priority
	-max:inst        <uint>           #            0 # maximum number of inst's to execute
	-bpred           <string>         #        bimod # branch predictor type {nottaken|taken|bimod|2lev|comb|oghel}
	-bpred:bimod     <int>            # 2048 # bimodal predictor config (<table size>)
	-bpred:2lev      <int list...>    # 1 1024 8 0 # 2-level predictor config (<l1size> <l2size> <hist_size> <xor>)
	-bpred:comb      <int>            # 1024 # combining predictor config (<meta_table_size>)
	-bpred:ras       <int>            #            8 # return address stack size (0 for no return stack)
	-bpred:btb       <int list...>    # 512 4 # BTB config (<num_sets> <associativity>)
	-bpred:oghel     <int list...>    # 7 2048 4 # OGHEL predictor config (<num_tables> <table_size> <counter_len>)

	Branch predictor configuration examples for 2-level predictor:
	Configurations:   N, M, W, X
	N   # entries in first level (# of shift register(s))
	W   width of shift register(s)
	M   # entries in 2nd level (# of counters, or other FSM)
	X   (yes-1/no-0) xor history and address for 2nd level index
	Sample predictors:
	GAg     : 1, W, 2^W, 0
	GAp     : 1, W, M (M > 2^W), 0
	PAg     : N, W, 2^W, 0
	PAp     : N, W, M (M == 2^(N+W)), 0
	gshare  : 1, W, 2^W, 1
	Predictor `comb' combines a bimodal and a 2-level predictor.


\end{verbatim}
}

\subsection{Prezentarea predictorului OGhel}

Predictia ramificatiilor reprezinta una dintre tehnicile fundamentale de optimizare a performantei procesoarelor moderne cu pipeline adanc.
In absenta unei predictii corecte, procesorul este nevoit sa goleasca pipeline-ul si sa reia executia de la adresa corecta,
operatie cunoscuta sub numele de \textit{branch misprediction penalty}. De-a lungul timpului, cercetatorii au propus o
varietate de strategii cu complexitati si acurateti diferite.

\textbf{Predictori statici si bazati pe Markov.}
Cei mai simpli predictori sunt cei statici: \textit{always taken} si \textit{always not-taken}, care nu tin cont de istoricul executiei.
O treapta superioara o reprezinta predictorul bazat pe \textbf{lanturi Markov}, care modeleaza probabilitatea urmatorului rezultat
al unui salt in functie de cele $k$ rezultate anterioare ale aceluiasi salt. Desi intuitiv, modelul Markov este limitat de faptul
ca ia in considerare exclusiv istoricul local al unui singur salt, ignorand corelatiile cu alte ramificatii din program.

\textbf{Predictori globali -- familia GAg/GAp/PAp.}
Schema \textbf{GAg} (\textit{Global history, All branches, global prediction table}) introduce notiunea de
\textit{registru de istorie globala} (GHR -- \textit{Global History Register}), in care se acumuleaza
rezultatele ultimelor $W$ salturi, indiferent de adresa acestora. Indicele in tabela de predictie se
formeaza direct din GHR. \textbf{GAp} pastreaza istoricul global, dar foloseste tabele de predictie
per-ramificatie, reducand aliasul dintre intrari. \textbf{PAp} merge mai departe, combinand un
registru de istorie per-ramificatie cu tabele de predictie individuale. Aceste scheme ofera o
acuratete ridicata pentru programe cu corelatii inter-ramificatii, insa tabela creste exponential
cu adancimea istoricului.

\textbf{Predictorul GShare si predictori combinati.}
\textbf{GShare} aplica operatia XOR intre GHR si adresa PC a saltului pentru a indexa tabela de predictie,
reducand aliasul fara a creste semnificativ dimensiunea structurii. Predictorii \textbf{combinati} (de tip \textit{tournament}),
cum este cel implementat in SimpleSim sub optiunea \texttt{comb}, mentin doua predictori de baza si o \textit{meta-tabela}
care decide dinamic caruia sa ii acorde incredere pentru fiecare salt in parte.

\textbf{Predictori bazati pe perceptron.}
O directie moderna si importanta este reprezentata de predictorii bazati pe \textbf{perceptron}. Fiecare ramificatie este asociata
unui vector de ponderi; rezultatul predictiei este determinat de semnul produsului scalar dintre vectorul de ponderi si vectorul de
istorie globala (codificat ca $\pm 1$). Antrenamentul consta in incrementarea sau decrementarea ponderilor corespunzatoare in functie
de rezultatul real. Avantajul principal este capacitatea de a captura corelatii liniare pe istorii foarte lungi, la un cost de stocare
rezonabil. Limita fundamentala a perceptronului simplu consta insa in incapacitatea de a invata functii nelineare: de exemplu, paritatea
unui sir de biti (corelatii XOR) nu poate fi reprezentata de un model liniar.

\textbf{Predictorul O-GEHL.}
\textbf{O-GEHL} (\textit{O-Geometric History Length}) depaseste limitarile mentionate mai sus printr-o serie de mecanisme specifice.
In locul unei singure tabele indexate dupa un istoric de lungime fixa, O-GEHL utilizeaza \textbf{mai multe tabele independente} $T_0, T_1, \ldots, T_{M-1}$,
fiecare indexata dupa un istoric de lungime diferita. Lungimile istoricului formeaza o \textbf{progresie geometrica}: $L_i = \lfloor \alpha^i \cdot L_0 \rceil$,
unde $\alpha > 1$ este un factor de scala. Aceasta alegere asigura acoperirea uniforma a fenomenelor de corelatie la scari multiple ale
istoricului, de la corelatie pe termen scurt pana la corelatie pe termen lung. Predictia finala se obtine prin \textbf{suma algebrica}
a valorilor contoarelor saturate extrase din fiecare tabela -- un mecanism similar votului ponderat al unui ansamblu de perceptroni,
dar fara a fi limitat la corelatii liniare, datorita indexarii diferentiate. Aceasta suma este comparata cu zero: daca este pozitiva,
saltul este prezis ca efectuat (\textit{taken}), altfel ca neefectuat. Spre deosebire de predictori globali simpli, \textbf{aliasul}
este gestionat prin diversificarea functiilor de indexare: fiecare tabela foloseste o combinatie diferita de biti din PC si din GHR,
reducand probabilitatea ca doua ramificatii distincte sa partajeze acelasi contor in toate tabelele simultan. In concluzie, O-GEHL
reuneste avantajele istoricului global (capturarea corelatiilor inter-ramificatii), ale lungimilor geometrice (acoperire multi-scala),
si ale votului prin suma (robustete la zgomot si alias), oferind o acuratete superioara predictorilor clasici la un buget de hardware comparabil.

\subsection{Parametrii predictorului GEHL}

Proiectarea unui predictor GEHL implică patru grade de libertate principale. Alegerea valorilor optime pentru fiecare parametru constituie un compromis
între acuratețe, cost hardware și latență.

\subsubsection*{Numărul de tabele $M$}

GEHL utilizează $M$ tabele independente, fiecare indexat după un istoric de lungime
diferită. Tabelul $T_0$ nu folosește istoric global (\textit{bias table}, $L[0]=0$),
iar tabelele $T_1,\ldots,T_{M-1}$ utilizează lungimi crescătoare.
Această diversificare permite capturarea atât a corelațiilor pe termen scurt, cât și
a celor pe termen lung.

Studiile empirice arată că intervalul $M \in [4,\,12]$ oferă cel mai bun raport
acuratețe\,/\,cost. Sub 4 tabele granularitatea istoricului este insuficientă; peste 12
câștigul marginal de precizie nu justifică costul hardware suplimentar
(\textit{diminishing returns}).

\subsubsection*{Contoarele saturate pe $k$ biți}

Fiecare intrare dintr-un tabel stochează un contor saturat pe $k$ biți, care codifică
„încrederea" predictorului pentru un anumit pattern de istoric.
Valoarea implicită $k=4$ reprezintă un compromis bun: $k=2$ este prea instabil, iar
$k\geq8$ risipește spațiu fără câștig semnificativ de acuratețe.

\subsubsection*{Seria geometrică a lungimilor de istoric}

Lungimile de istoric urmează o \textbf{progresie geometrică}:
\[
  L[i] = \left\lfloor \alpha^{\,i-1} \cdot L_1 + 0{,}5 \right\rfloor, \quad i = 1,\ldots,M-1
\]
unde $\alpha$ este factorul de creștere și $L_1$ este lungimea minimă de istoric
(implicit $\alpha=2$, $L_1=2$, rezultând seria $\{0,2,4,8,16,32,64,128\}$).

Față de o progresie liniară, cea geometrică acoperă eficient un interval larg de scări:
istorii scurte asigură granularitate fină, iar creșterea exponențială permite atingerea
rapidă a dependențelor îndepărtate. Valori extreme ale lui $\alpha$ sunt contraproductive:
$\alpha\geq3$ introduce goluri în acoperire, iar $\alpha\approx1{,}2$ produce tabele
redundante. Factorul $\alpha$ se poate calcula din lungimile extreme ale seriei:
\[
  \alpha = \left(\frac{L(M-1)}{L(1)}\right)^{\!\frac{1}{M-2}}
\]

\subsubsection*{Pragul de actualizare $\theta$}

Predictorul calculează suma algebrică a contoarelor din toate tabelele,
$S = \sum_{i=0}^{M-1} T_i$,
și prezice \textit{taken} dacă $S \geq 0$, altfel \textit{not taken}.
Contoarele sunt actualizate doar la mispredictie sau când $|S| \leq \theta$,
evitând modificări inutile atunci când predictorul este deja sigur.

Valoarea implicită $\theta \approx M$ este justificată de faptul că suma maximă crește
proporțional cu numărul de tabele. Un $\theta$ prea mic conduce la actualizări excesive
(instabilitate, overfitting); un $\theta$ prea mare încetinește adaptarea predictorului.

\subsubsection*{Fluxul de functionare}

\paragraph{Predictie.}
Pentru un PC dat, fiecare tabel $i$ calculeaza un index:
\[
  \mathit{idx}_i = \bigl(PC \oplus \mathit{GHR}[0:L[i]]\bigr) \bmod \mathit{tableSize}
\]
Valoarea contorului de la acel index, $c_i$, este adaugata la suma globala:
\[
  S = \sum_{i=0}^{M-1} c_i[\,\mathit{idx}_i\,]
\]
Decizia finala de predictie este:
\[
  \hat{y} =
  \begin{cases}
    \textit{taken}     & \text{dacă } S \geq 0 \\
    \textit{not taken} & \text{dacă } S < 0
  \end{cases}
\]

\paragraph{Actualizare.}
Dupa rezolvarea branch-ului, predictorul executa trei operatii in ordine:

\begin{enumerate}
    \item \textbf{Actualizarea contoarelor de predictie}: daca a existat
          o mispredictie sau daca predictorul are incredere redusa in
          predictie ($|\mathit{sum}| \leq \theta$), fiecare contor accesat
          este actualizat prin incrementare (pentru branch \textit{taken})
          sau decrementare (pentru branch \textit{not taken}).

          Ideea este ca predictorul invata agresiv doar atunci cand:
          \begin{itemize}
              \item predictia a fost gresita; sau
              \item predictia a fost corecta, dar confidence-ul a fost mic.
          \end{itemize}

          Astfel se evita modificarea inutila a contoarelor atunci cand
          predictorul este deja foarte sigur de decizie.

    \item \textbf{Ajustarea dinamica a lungimii istoriei}
          (\textit{Dynamic History Length Fitting}): mecanismul monitorizeaza
          mispredictiile recente pentru a detecta daca acestea sunt cauzate
          de aliasing (coliziuni intre istorii diferite care acceseaza
          aceeasi intrare din tabel).

          Daca aliasing counter-ul satureaza pozitiv, predictorul considera
          ca istoricul curent este insuficient pentru capturarea corelatiilor
          si activeaza un GHR (\textit{Global History Register}) mai lung
          pentru tabelele cu indici pari. Daca aliasing counter-ul satureaza
          negativ, predictorul revine la un GHR mai scurt pentru reducerea
          zgomotului si a aliasing-ului.

          Comutarea doar pentru jumatate dintre tabele permite coexistenta:
          \begin{itemize}
              \item corelatiilor locale (istorii scurte);
              \item corelatiilor pe termen lung (istorii lungi).
          \end{itemize}

          Astfel predictorul se poate adapta dinamic la diferite aplicatii
          sau chiar la faze diferite ale aceluiasi program.

    \item \textbf{Ajustarea adaptiva a pragului $\theta$}
          (\textit{Adaptive Threshold Fitting}): pragul $\theta$ controleaza
          confidence-ul minim necesar pentru a evita actualizarea predictorului.

          Predictorul produce o suma:

          \[
              \mathit{sum} = \sum_i T_i
          \]

          iar magnitudinea $|\mathit{sum}|$ reprezinta nivelul de incredere
          al predictiei.

          Threshold counter-ul este incrementat la mispredictie si decrementat
          atunci cand predictia este corecta, dar confidence-ul este redus
          ($|\mathit{sum}| \leq \theta$). La saturare, valoarea lui $\theta$
          este ajustata automat:

          \begin{itemize}
              \item cresterea lui $\theta$ face predictorul mai agresiv,
                    permitand mai multe update-uri;
              \item scaderea lui $\theta$ face predictorul mai conservator,
                    reducand update-urile inutile.
          \end{itemize}

          Acest mecanism permite adaptarea dinamica a sensibilitatii
          predictorului la comportamentul curent al aplicatiei si reduce
          atat zgomotul, cat si supra-antrenarea predictorului.
\end{enumerate}

\subsubsection*{Parametrii predictorului}

Implementarea expune trei parametri configurabili, prezentati in
Tabelul~\ref{tab:params}.

\begin{table}[H]
    \centering
    \small
    \begin{tabular}{|l|c|c|c|l|}
        \hline
        \textbf{Parametru} & \textbf{Implicit} & \textbf{Min} & \textbf{Max} & \textbf{Descriere} \\
        \hline
        Numar de tabele ($N$)      & 7    & 4 & 12    & Numarul de tabele de predictie \\
        Marimea tabelelor          & 2048 & 1 & 88000 & Numarul de intrari per tabel \\
        Lungimea contoarelor ($k$) & 4    & 3 & 5     & Precizia contoarelor saturate (biti) \\
        \hline
        $\alpha$ (raport geometric) & 2   & -- & --   & Factorul de crestere al istoriei \\
        $L_1$ (istorie minima)      & 2   & -- & --   & Lungimea istoriei pentru tabelul 1 \\
        $\theta$ initial            & $N$ & -- & --   & Pragul adaptiv de actualizare \\
        \hline
    \end{tabular}
    \caption{Parametrii predictorului OGEHL}
    \label{tab:params}
\end{table}


\subsection{Compiland OpenOGEHL}
Asa cum am promis in capitolul anterior, este timpul sa prezentam succint modul in care OpenOGEHL (fie in varianta ei clasica sau ca modul integrat) este compilat.\\

Avand in vedere ca proiectul implementat de noi si SimpleSim au fost scrise folosind limbajul de programare C, nu ne bucuram de facilitatea unui \textit{package manager}, adica a unui utilitar ce poate gestiona pachetele/modulele/bibliotecile folosite de aplicatia noastra si dependintele acestora. Un bun exemplu de astfel de utilitar este \textbf{cargo}, folosit in limbajul Rust. Acesta este responsabil de instalarea modulelor pe calculatorul programatorului si fixarea versiunilor acestora. Limbajul C in schimb, este mai vechi si a fost conceput a fi minimalist si simplu (structural vorbind, nu neaparat ca uz), astfel acesta se bazeaza pe manipularea directa a driver-ului GCC (GNU Compiler Collection) pentru a compila aplicatia. Preferam sa folosim termenul driver si nu compilator deoarece GCC intern alcatui din preprocesor (cpp), compilator (cc1), asamblor (as) si link-editor(ld), care practic corespond etapelor procesului de compilare a oricarui program scris in C.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/gcc.jpg}
	\caption{Structura interna a driver-ului GCC}
\end{figure}

Totusi, pentru a face mai lesne acest proces de compilare a aplicatilor, programatorii tind sa foloseasca asa numitul \textit{build system} pe care l-am mentionat in capitolul anterior. In cazul limbajului C cele mai populare solutii sunt \textbf{Make} si \textbf{CMake}. In timp ce cel dintai sistem este mai degraba un procesor de text (flexibil si cu multe facilitati totusi), cel de al doilea reprezinta o solutie moderna aplicata in mai multe proiecte (cum ar fi de exemplu Raylib, biblioteca grafica folosita pentru OpenOGEHL clasic). Mecanismele si principiile prin care Make si CMake nu vor fi elaborate nefiind scopul acestei lucrari. Ne vom multumi insa sa precizam ca CMake genereaza la nivel intermediar scripturi de Make, urmand apoi a fi rulate pentru a finaliza procesul de compilare.\\

Ca urmare a efortului nostru de a integra predictorul OGEHL in cadrul SimpleSim am hotarat sa uniformizam sistemele de build folosite, convertind astfel scriptul de Make existent dupa cum urmeaza:

\begin{verbatim}
	cmake_minimum_required(VERSION 3.16)
	project(simplesim C)

	set(CMAKE_C_STANDARD 99)

	# ----------------------------------------------------------------------------
	# Compile flags matching the original Makefile defaults
	# MFLAGS: from sysprobe (little-endian Linux x86-64)
	# FFLAGS: -DDEBUG
	# OFLAGS: -O0 -g -Wall
	# ----------------------------------------------------------------------------
	set(SIMPLESIM_DEFS
	BYTES_LITTLE_ENDIAN
	WORDS_LITTLE_ENDIAN
	FAST_SRL
	FAST_SRA
	GZIP_PATH="/bin/gzip"
	DEBUG
	)

	set(SIMPLESIM_FLAGS -O0 -g -Wall -w)

	# ----------------------------------------------------------------------------
	# Target: target-pisa (hard-coded; replaces the old config-pisa symlinks)
	# The top-level simplesim dir already has symlinks:
	#   config.h  -> target-pisa/config.h
	#   machine.h -> target-pisa/pisa.h
	#   machine.c -> target-pisa/pisa.c
	#   machine.def -> target-pisa/pisa.def
	#   loader.c  -> target-pisa/loader.c
	#   symbol.c  -> target-pisa/symbol.c
	#   syscall.c -> target-pisa/syscall.c
	# ----------------------------------------------------------------------------

	# Include directories: the simplesim root so that machine.h / config.h resolve
	set(SIMPLESIM_ROOT ${CMAKE_CURRENT_SOURCE_DIR})
	set(OGHEL_ROOT     ${CMAKE_CURRENT_SOURCE_DIR}/../src/bp/)

	# ----------------------------------------------------------------------------
	# libexo static library
	# exolex.c is a pre-generated flex output already present in the repo.
	# ----------------------------------------------------------------------------
	add_library(exo STATIC
	libexo/libexo.c
	libexo/exolex.c
	)
	target_include_directories(exo PUBLIC ${SIMPLESIM_ROOT})
	target_compile_definitions(exo PRIVATE ${SIMPLESIM_DEFS})
	target_compile_options(exo PRIVATE ${SIMPLESIM_FLAGS})

	# ----------------------------------------------------------------------------
	# Common objects shared by all simulators
	# (main.c, plus the platform-independent support files)
	# sysprobe.c is excluded - it is a standalone host-probe utility.
	# ----------------------------------------------------------------------------
	set(SIMPLESIM_COMMON_SRCS
	src/main.c
	src/syscall.c
	src/memory.c
	src/regs.c
	src/loader.c
	src/endian.c
	src/dlite.c
	src/symbol.c
	src/eval.c
	src/options.c
	src/stats.c
	src/eio.c
	src/range.c
	src/misc.c
	src/machine.c
	src/eventq.c
	src/resource.c
	)

	set(OGHEL_COMMON_SRCS
	${OGHEL_ROOT}/bp.c
	${OGHEL_ROOT}/utils.c
	)

	# ----------------------------------------------------------------------------
	# sim-bpred
	# Extra sources: sim-bpred.c, bpred.c
	# ----------------------------------------------------------------------------
	add_executable(sim-bpred
	sim-bpred.c
	bpred.c
	${SIMPLESIM_COMMON_SRCS}
	${OGHEL_COMMON_SRCS}
	)
	target_include_directories(sim-bpred PRIVATE ${SIMPLESIM_ROOT}/inc/)
	target_include_directories(sim-bpred PRIVATE ${OGHEL_ROOT})
	target_compile_definitions(sim-bpred PRIVATE ${SIMPLESIM_DEFS})
	target_compile_options(sim-bpred PRIVATE ${SIMPLESIM_FLAGS})
	target_link_libraries(sim-bpred PRIVATE exo m)

\end{verbatim}

\subsubsection{Arhitectura software OpenOGEHL clasic}
Pentru a asigura o buna calitate software a aplicatiei si potentiala extindere a acesteia in diferite directii, s-a pus problema intocmirii unei arhitecturi simple dar robuste. Vom discuta in cele ce urmeaza cum a fost aceasta realizata pentru aplicatia clasica de OpenOGEHL, aceste decizii fiind valabile intr-o masura sau alta si in momentul integrarii cu SimpleSim.\\

In ansamblu, aplicatia este constituita din urmatoarele componente principale: fisierele de tip trace ce conțin traseul parcurs de program, interfata grafica, responsabila pentru redarea utilizatorului a informatiilor necesare, preluarea configurarilor acestuia si transmiterea datelor necesare predictorului de salt (ex: numarul de tabele folosit), si intr-un final, predictorul de salturi in sine, ce implementeaza algoritmul O-GEHL discutat anterior, responsabila de a asemenea cu returnarea catre interfata grafica a ratei de miss (cat la suta din numarul prezis de salturi nu s-au efectuat de fapt)

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/arch0.png}
	\caption{Componentele principale}
\end{figure}

La nivel de implementare software aplicatia pleaca de la un punct de start (numit deseori entry point in literatura de specialitate) dupa care se bifurca in doua fire de executie, unul pentru interfata grafica si altul pentru predictorul de salturi (abreviat drept BP). Aceasta decizie a fost luata din considerente de performanta si experienta a utilizatorului, anume sarcinile de calcul a preditiei care implica un consum de resurse mare de resurse au fost izolate de cele de redare a interfetei grafice. Evident, acest avantaj este valabil doar in sistemele avand procesoare cu mai multe nuclee (altfel, in loc de executie paralela are loc executie concurenta). Data fiind natura
bibliotecilor grafice folosite (raylib, respectiv raygui) firul principal
de executie revine interfetei cu utilizatorul.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/arch1.png}
	\caption{Interactiunea in timp a sistemului}
\end{figure}

\subsubsection{Arhitectura software OpenOGEHL integrat}
Fiind acum familiari despre cum a fost aplicatia proiectata initial, este momentul sa discutam despre masurile luate pentru integrarea acesteia. Daca este sa ne aducem aminte de arhitectura SimpleSim vom observa usor ca predictorul nostru de ramificatii este situat la nivelul cel mai de jos, adica la nivelul Performance Core (responsabil pentru redarea comportamentului executiei micro-arhitecturii procesorului).Astfel, componenta grafica implementata de noi nu ne mai este folositoare, caci SimpleSim ofera deja utilizatorului o anumita interfata cu care poate lucra. Nemaifiind nevoie de un GUI evident ca putem simplifica structura si mai mult prin eliminarea firelor de executie, ramanand astfel doar cu logica ce implementeaza comportamentul unui predictor tip O-GEHL. \\


\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/sim_drawio.png}
	\caption{OpenOGEHL ca parte a SimpleScalar}
\end{figure}


Din perspectiva programarii, fiecare modul din SimpleScalar este alcatuit dintr-un fisier antet (*.h) ce defineste interfata disponibila utilizatorului si un fisier de implementare (*.c). Cu exceptia a catorva fisiere *.h OpenOGEHL nu se abata de la aceasta regula si poate fi folosit in program prin simpla includere a antetului \textit{bp.h}. In cazul acestui proiect modulul este inclus ca parte a \textit{bpred} conform figurei urmatoare:

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{images/arch2.png}
	\caption{Includerea OpenOGEHL la nivel de fisier}
\end{figure}

Aceasta includere nici macar nu este resimtita de catre programator deoarece din punctul sau de vedere el sau ea lucreaza doar cu componenta bpred. Evident, data fiind natura limbajului C in care nu exista o separare rigida intre interfata si implementare (cum ar fi de exemplu cazul limbajului C++), nimic nu impiedica utilizarea directa a OpenOGEHL a aplicatiei trecand astfel peste un nivel intermediar.

Asa ca, ca daca ar fi sa privim prin prisma formalismului interfata-implementare, observam ca urmatoarele functii sunt puse la dispozitie:

\begin{itemize}
	\item BP\_Init(): responsabila pentru initializarea tabelelor din predictor
	\item BP\_GetPrediction(): responsabila prezicerea saltului
	\item BP\_Update(): responsabila pentru actualizarea contoarelor din tabele
\end{itemize}

In cele ce urmeaza vom prezenta logica din spatele acestei interfete oferite de aplicatie.\\

\textbf{BP\_Init}\\
Funtie responsabila de initializarea parametrilor interni ai predictorului si anume:
\begin{itemize}
	\item bpTable: tablou ce inglobeaza toate tabelele predictorului
	\item aliasingCounter: contor folosit pentru a determina daca se va folosi istoria local sau globala
	\item theta\_threshold: variabila ce memoreaza pragul folosit pentru a decide daca contoarele din tabelele predictorului vor fi actualizate sau nu.
	\item ghr: serveste drept model pentru registrul GHR (Global History Register) 
\end{itemize}

\textbf{BP\_GetPrediction}
Functie ce are ca parametru de intrare valoarea curenta a registrului PC, valoare ce va fi folosita in selectia tabelelor corespunzatoare. Aceasta selectie se face pe baza aplicarii operatiei logic SAU EXCLUSIV sau XOR intre ce se afla in PC si indexul tabelelor ce contin contoarele (a se vedea apelul functiei BP\_GetCntIdx). Dupa cum s-a mentionat anterior, algoritmul mentioneaza nevoia de a calcula suma 
algebrica a contoarelor pentru a decide daca un salt se va face sau nu (da daca suma este pozitiva si nu altfel).

\textbf{BP\_Update}
Functie ce are ca parametri de intrare rezultatul predictiei (dacă se face sau nu saltul), rezultatul real, valoarea curenta a registrului PC împreuna cu valoarea adresei de salt. Aceasta funcție actualizeaza starea predictorului dupa ce predictia pentru salt a avut loc. Actualizăaile au loc daca predictorul a prezis incorect sau suma algebrica a ajuns sub nivelul pragului $\theta$ stabilit.

